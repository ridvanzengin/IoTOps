fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
motor==3.6.1
tomli-w==1.1.0
docker==7.1.0
asyncpg==0.30.0
httpx==0.28.1
celery[redis]==5.4.0
sse-starlette==2.1.3
# pymongo/redis were already transitive deps (via motor/celery[redis]) --
# pinned explicitly now that app/automater/tasks.py imports them directly
# (a sync Mongo/Redis client for the Celery worker, which can't share the
# rest of the app's async motor/redis clients). Versions match what motor
# 3.6.1/celery[redis] 5.4.0 already resolve, not a new constraint.
pymongo==4.9.2
redis==5.3.1
# Cron next-run semantics for QueryRuleSchedule's cron option
# (app/query_rule/service.py) -- real day-of-week/month-boundary handling
# isn't worth hand-rolling. Small, pure-Python, no C extension.
croniter==6.2.4
# Official Anthropic SDK -- default backend for every AI feature
# (app/ai/providers/anthropic_provider.py). See google-genai below for the
# free-tier alternative, selected via Settings.ai_provider.
anthropic==0.116.0
# Official Google Gen AI SDK -- Gemini-backed alternative to Anthropic
# (app/ai/providers/gemini_provider.py), for when the Anthropic budget runs
# out. Not used unless AI_PROVIDER=gemini in .env.
google-genai==2.12.1
